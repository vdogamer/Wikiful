<h1>This is the index view for Categories</h1>
<% @categories.each do |category| %>
  <div>
    <h3><%= link_to category.name, category %></h3>
    
    <p>Created at <%= category.created_at.strftime('%b %d, %Y') %></p>
</div>
<% end %>